fips_begin_app(z1013 windowed)
    fips_files(z1013.c)
    if (FIPS_IOS)
        fips_files(ios-info.plist)
    endif()
    fips_deps(roms common)
fips_end_app()
fips_begin_app(z1013-ui windowed)
    fips_files(z1013.c z1013-ui-impl.cc)
    if (FIPS_IOS)
        fips_files(ios-info.plist)
    endif()
    fips_deps(roms common ui)
fips_end_app()
target_compile_definitions(z1013-ui PRIVATE CHIPS_USE_UI)

fips_begin_app(z9001 windowed)
    fips_files(z9001.c)
    if (FIPS_IOS)
        fips_files(ios-info.plist)
    endif()
    fips_deps(roms common)
fips_end_app()
fips_begin_app(z9001-ui windowed)
    fips_files(z9001.c z9001-ui-impl.cc)
    if (FIPS_IOS)
        fips_files(ios-info.plist)
    endif()
    fips_deps(roms common ui)
fips_end_app()
target_compile_definitions(z9001-ui PRIVATE CHIPS_USE_UI)

fips_begin_app(atom windowed)
    fips_files(atom.c)
    if (FIPS_IOS)
        fips_files(ios-info.plist)
    endif()
    fips_deps(roms common)
fips_end_app()
fips_begin_app(atom-ui windowed)
    fips_files(atom.c atom-ui-impl.cc)
    if (FIPS_IOS)
        fips_files(ios-info.plist)
    endif()
    fips_deps(roms common ui)
fips_end_app()
target_compile_definitions(atom-ui PRIVATE CHIPS_USE_UI)

fips_begin_app(c64 windowed)
    fips_files(c64.c)
    if (FIPS_IOS)
        fips_files(ios-info.plist)
    endif()
    fips_deps(roms common)
fips_end_app()
fips_begin_app(c64-ui windowed)
    fips_files(c64.c c64-ui-impl.cc)
    if (FIPS_IOS)
        fips_files(ios-info.plist)
    endif()
    fips_deps(roms common ui)
fips_end_app()
target_compile_definitions(c64-ui PRIVATE CHIPS_USE_UI)

fips_begin_app(vic20 windowed)
    fips_files(vic20.c)
    if (FIPS_IOS)
        fips_files(ios-info.plist)
    endif()
    fips_deps(roms common)
fips_end_app()
fips_begin_app(vic20-ui windowed)
    fips_files(vic20.c vic20-ui.cc)
    if (FIPS_IOS)
        fips_files(ios-info.plist)
    endif()
    fips_deps(roms common ui)
fips_end_app()
target_compile_definitions(vic20-ui PRIVATE CHIPS_USE_UI)

fips_begin_app(zx windowed)
    fips_files(zx.c)
    if (FIPS_IOS)
        fips_files(ios-info.plist)
    endif()
    fips_deps(roms common)
fips_end_app()
fips_begin_app(zx-ui windowed)
    fips_files(zx.c zx-ui-impl.cc)
    if (FIPS_IOS)
        fips_files(ios-info.plist)
    endif()
    fips_deps(roms common ui)
fips_end_app()
target_compile_definitions(zx-ui PRIVATE CHIPS_USE_UI)

fips_begin_app(cpc windowed)
    fips_files(cpc.c)
    if (FIPS_IOS)
        fips_files(ios-info.plist)
    endif()
    fips_deps(roms common)
fips_end_app()
fips_begin_app(cpc-ui windowed)
    fips_files(cpc.c cpc-ui-impl.cc)
    if (FIPS_IOS)
        fips_files(ios-info.plist)
    endif()
    fips_deps(roms common ui)
fips_end_app()
target_compile_definitions(cpc-ui PRIVATE CHIPS_USE_UI)

fips_begin_app(kc852 windowed)
    fips_files(kc85.c)
    if (FIPS_IOS)
        fips_files(ios-info.plist)
    endif()
    fips_deps(roms common)
fips_end_app()
target_compile_definitions(kc852 PRIVATE CHIPS_KC85_TYPE_2)
fips_begin_app(kc852-ui windowed)
    fips_files(kc85.c kc85-ui-impl.cc)
    if (FIPS_IOS)
        fips_files(ios-info.plist)
    endif()
    fips_deps(roms common ui)
fips_end_app()
target_compile_definitions(kc852-ui PRIVATE CHIPS_KC85_TYPE_2)
target_compile_definitions(kc852-ui PRIVATE CHIPS_USE_UI)

fips_begin_app(kc853 windowed)
    fips_files(kc85.c)
    if (FIPS_IOS)
        fips_files(ios-info.plist)
    endif()
    fips_deps(roms common)
fips_end_app()
target_compile_definitions(kc853 PRIVATE CHIPS_KC85_TYPE_3)
fips_begin_app(kc853-ui windowed)
    fips_files(kc85.c kc85-ui-impl.cc)
    if (FIPS_IOS)
        fips_files(ios-info.plist)
    endif()
    fips_deps(roms common ui)
fips_end_app()
target_compile_definitions(kc853-ui PRIVATE CHIPS_KC85_TYPE_3)
target_compile_definitions(kc853-ui PRIVATE CHIPS_USE_UI)

fips_begin_app(kc854 windowed)
    fips_files(kc85.c)
    if (FIPS_IOS)
        fips_files(ios-info.plist)
    endif()
    fips_deps(roms common)
fips_end_app()
target_compile_definitions(kc854 PRIVATE CHIPS_KC85_TYPE_4)
fips_begin_app(kc854-ui windowed)
    fips_files(kc85.c kc85-ui-impl.cc)
    if (FIPS_IOS)
        fips_files(ios-info.plist)
    endif()
    fips_deps(roms common ui)
fips_end_app()
target_compile_definitions(kc854-ui PRIVATE CHIPS_KC85_TYPE_4)
target_compile_definitions(kc854-ui PRIVATE CHIPS_USE_UI)

fips_begin_app(bombjack windowed)
    fips_files(bombjack.c)
    if (FIPS_IOS)
        fips_files(ios-info.plist)
    endif()
    fips_deps(roms common)
fips_end_app()
fips_begin_app(bombjack-ui windowed)
    fips_files(bombjack.c bombjack-ui-impl.cc)
    if (FIPS_IOS)
        fips_files(ios-info.plist)
    endif()
    fips_deps(roms common ui)
fips_end_app()
target_compile_definitions(bombjack-ui PRIVATE CHIPS_USE_UI)

fips_begin_app(pacman windowed)
    fips_files(pacman.c)
    if (FIPS_IOS)
        fips_files(ios-info.plist)
    endif()
    fips_deps(roms common)
fips_end_app()
fips_begin_app(pacman-ui windowed)
    fips_files(pacman.c pacman-ui-impl.cc)
    if (FIPS_IOS)
        fips_files(ios-info.plist)
    endif()
    fips_deps(roms common ui)
fips_end_app()
target_compile_definitions(pacman-ui PRIVATE CHIPS_USE_UI)

fips_begin_app(pengo windowed)
    fips_files(pengo.c)
    if (FIPS_IOS)
        fips_files(ios-info.plist)
    endif()
    fips_deps(roms common)
fips_end_app()
fips_begin_app(pengo-ui windowed)
    fips_files(pengo.c pengo-ui-impl.cc)
    if (FIPS_IOS)
        fips_files(ios-info.plist)
    endif()
    fips_deps(roms common ui)
fips_end_app()
target_compile_definitions(pengo-ui PRIVATE CHIPS_USE_UI)

fips_begin_app(lc80 windowed)
    fips_files(lc80.c lc80-ui-impl.cc)
    if (FIPS_IOS)
        fips_files(ios-info.plist)
    endif()
    fips_deps(roms common ui)
fips_end_app()
