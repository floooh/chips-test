# select the sokol-gfx backend depending on platform
if(FIPS_EMSCRIPTEN OR FIPS_RASPBERRYPI OR FIPS_ANDROID)
    add_definitions(-DSOKOL_GLES2)
elseif(FIPS_WINDOWS)
    add_definitions(-DSOKOL_D3D11)
elseif(FIPS_OSX)
    add_definitions(-DSOKOL_METAL)
else()
    add_definitions(-DSOKOL_GLCORE33)
endif()

if (FIPS_WINDOWS)
    add_definitions(-D_CRT_SECURE_NO_WARNINGS)
endif()

# a lib of common code shared between all emulators
fips_begin_lib(common)
    fips_vs_warning_level(3)
    fips_dir(common)
    fips_files(common.c common.h)
    fips_files(args.h clock.h fs.h gfx.h keybuf.h)
    if (FIPS_OSX)
        fips_files(sokol.m)
        if (FIPS_IOS)
            fips_frameworks_osx(UIKit Metal MetalKit AudioToolbox)
        else()
            fips_frameworks_osx(Cocoa QuartzCore Metal MetalKit AudioToolbox)
        endif()
    else()
        fips_files(sokol.c)
        if (FIPS_LINUX)
            fips_libs(X11 GL m dl)
        endif()
    endif()
fips_end_lib()

fips_begin_app(z1013 windowed)
    fips_vs_warning_level(3)
    fips_files(z1013.c)
    if (FIPS_IOS)
        fips_files(ios-info.plist)
    endif()
    fips_dir(roms)
    fips_generate(FROM z1013-roms.yml TYPE dump SOURCE z1013-roms.c HEADER z1013-roms.h)
    fips_deps(common)
fips_end_app()

fips_begin_app(z9001 windowed)
    fips_vs_warning_level(3)
    fips_files(z9001.c)
    if (FIPS_IOS)
        fips_files(ios-info.plist)
    endif()
    fips_dir(roms)
    fips_generate(FROM z9001-roms.yml TYPE dump SOURCE z9001-roms.c HEADER z9001-roms.h)
    fips_deps(common)
fips_end_app()

fips_begin_app(atom windowed)
    fips_vs_warning_level(3)
    fips_files(atom.c)
    if (FIPS_IOS)
        fips_files(ios-info.plist)
    endif()
    fips_dir(roms)
    fips_generate(FROM atom-roms.yml TYPE dump SOURCE atom-roms.c HEADER atom-roms.h)
    fips_deps(common)
fips_end_app()

fips_begin_app(c64 windowed)
    fips_vs_warning_level(3)
    fips_files(c64.c)
    if (FIPS_IOS)
        fips_files(ios-info.plist)
    endif()
    fips_dir(roms)
    fips_generate(FROM c64-roms.yml TYPE dump SOURCE c64-roms.c HEADER c64-roms.h)
    fips_deps(common)
fips_end_app()

fips_begin_app(zx windowed)
    fips_vs_warning_level(3)
    fips_files(zx.c)
    if (FIPS_IOS)
        fips_files(ios-info.plist)
    endif()
    fips_dir(roms)
    fips_generate(FROM zx-roms.yml TYPE dump SOURCE zx-roms.c HEADER zx-roms.h)
    fips_deps(common)
fips_end_app()

fips_begin_app(cpc windowed)
    fips_vs_warning_level(3)
    fips_files(cpc.c)
    if (FIPS_IOS)
        fips_files(ios-info.plist)
    endif()
    fips_dir(roms)
    fips_generate(FROM cpc-roms.yml TYPE dump SOURCE cpc-roms.c HEADER cpc-roms.h)
    fips_deps(common)
fips_end_app()
