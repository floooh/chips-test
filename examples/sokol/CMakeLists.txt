fips_begin_app(z1013 windowed)
    fips_vs_warning_level(3)
    fips_files(z1013.c)
    if (FIPS_IOS)
        fips_files(ios-info.plist)
    endif()
    fips_deps(z1013-roms common)
fips_end_app()

fips_begin_app(z9001 windowed)
    fips_vs_warning_level(3)
    fips_files(z9001.c)
    if (FIPS_IOS)
        fips_files(ios-info.plist)
    endif()
    fips_deps(z9001-roms common)
fips_end_app()

fips_begin_app(atom windowed)
    fips_vs_warning_level(3)
    fips_files(atom.c)
    if (FIPS_IOS)
        fips_files(ios-info.plist)
    endif()
    fips_deps(atom-roms common)
fips_end_app()

fips_begin_app(c64 windowed)
    fips_vs_warning_level(3)
    fips_files(c64.c)
    if (FIPS_IOS)
        fips_files(ios-info.plist)
    endif()
    fips_deps(c64-roms common)
fips_end_app()

fips_begin_app(zx windowed)
    fips_vs_warning_level(3)
    fips_files(zx.c)
    if (FIPS_IOS)
        fips_files(ios-info.plist)
    endif()
    fips_deps(zx-roms common)
fips_end_app()

fips_begin_app(cpc windowed)
    fips_vs_warning_level(3)
    fips_files(cpc.c)
    if (FIPS_IOS)
        fips_files(ios-info.plist)
    endif()
    fips_deps(cpc-roms common)
fips_end_app()

fips_begin_app(kc85 windowed)
    fips_vs_warning_level(3)
    fips_files(kc85.c)
    if (FIPS_IOS)
        fips_files(ios-info.plist)
    endif()
    fips_deps(kc85-roms common)
fips_end_app()
