fips_begin_app(z1013 windowed)
    fips_vs_warning_level(3)
    fips_files(z1013.c)
    if (FIPS_IOS)
        fips_files(ios-info.plist)
    endif()
    fips_deps(roms common)
fips_end_app()

fips_begin_app(z9001 windowed)
    fips_vs_warning_level(3)
    fips_files(z9001.c)
    if (FIPS_IOS)
        fips_files(ios-info.plist)
    endif()
    fips_deps(roms common)
fips_end_app()

fips_begin_app(atom windowed)
    fips_vs_warning_level(3)
    fips_files(atom.c)
    if (FIPS_IOS)
        fips_files(ios-info.plist)
    endif()
    fips_deps(roms common)
fips_end_app()

fips_begin_app(c64 windowed)
    fips_vs_warning_level(3)
    fips_files(c64.c)
    if (FIPS_IOS)
        fips_files(ios-info.plist)
    endif()
    fips_deps(roms common)
fips_end_app()

fips_begin_app(zx windowed)
    fips_vs_warning_level(3)
    fips_files(zx.c)
    if (FIPS_IOS)
        fips_files(ios-info.plist)
    endif()
    fips_deps(roms common)
fips_end_app()

fips_begin_app(cpc windowed)
    fips_vs_warning_level(3)
    fips_files(cpc.c)
    if (FIPS_IOS)
        fips_files(ios-info.plist)
    endif()
    fips_deps(roms common)
fips_end_app()

fips_begin_app(kc85 windowed)
    fips_vs_warning_level(3)
    fips_files(kc85.c)
    if (FIPS_IOS)
        fips_files(ios-info.plist)
    endif()
    fips_deps(roms common)
fips_end_app()

fips_begin_app(bombjack windowed)
    fips_vs_warning_level(3)
    fips_files(bombjack.c)
    if (FIPS_IOS)
        fips_files(ios-info.plist)
    endif()
    fips_deps(roms common)
fips_end_app()
