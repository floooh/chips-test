fips_ide_group(examples/z1013)
fips_begin_app(z1013 windowed)
    fips_vs_warning_level(3)
    fips_files(z1013.c)
    if (FIPS_IOS)
        fips_files(ios-info.plist)
    endif()
    fips_deps(roms common)
fips_end_app()
fips_begin_app(z1013-ui windowed)
    fips_vs_warning_level(3)
    fips_files(z1013.c z1013-ui.cc)
    if (FIPS_IOS)
        fips_files(ios-info.plist)
    endif()
    fips_deps(roms common ui)
fips_end_app()
target_compile_definitions(z1013-ui PRIVATE CHIPS_USE_UI)

fips_ide_group(examples/z9001)
fips_begin_app(z9001 windowed)
    fips_vs_warning_level(3)
    fips_files(z9001.c)
    if (FIPS_IOS)
        fips_files(ios-info.plist)
    endif()
    fips_deps(roms common)
fips_end_app()
fips_begin_app(z9001-ui windowed)
    fips_vs_warning_level(3)
    fips_files(z9001.c z9001-ui.cc)
    if (FIPS_IOS)
        fips_files(ios-info.plist)
    endif()
    fips_deps(roms common ui)
fips_end_app()
target_compile_definitions(z9001-ui PRIVATE CHIPS_USE_UI)

fips_ide_group(examples/atom)
fips_begin_app(atom windowed)
    fips_vs_warning_level(3)
    fips_files(atom.c)
    if (FIPS_IOS)
        fips_files(ios-info.plist)
    endif()
    fips_deps(roms common)
fips_end_app()
fips_begin_app(atom-ui windowed)
    fips_vs_warning_level(3)
    fips_files(atom.c atom-ui.cc)
    if (FIPS_IOS)
        fips_files(ios-info.plist)
    endif()
    fips_deps(roms common ui)
fips_end_app()
fips_begin_app(atomx windowed)
    fips_vs_warning_level(3)
    fips_files(atomx.c)
    if (FIPS_IOS)
        fips_files(ios-info.plist)
    endif()
    fips_deps(roms common)
fips_end_app()
target_compile_definitions(atom-ui PRIVATE CHIPS_USE_UI)

fips_ide_group(examples/c64)
fips_begin_app(c64 windowed)
    fips_vs_warning_level(3)
    fips_files(c64.c)
    if (FIPS_IOS)
        fips_files(ios-info.plist)
    endif()
    fips_deps(roms common)
fips_end_app()
fips_begin_app(c64-ui windowed)
    fips_vs_warning_level(3)
    fips_files(c64.c c64-ui.cc)
    if (FIPS_IOS)
        fips_files(ios-info.plist)
    endif()
    fips_deps(roms common ui)
fips_end_app()
fips_begin_app(c64x windowed)
    fips_vs_warning_level(3)
    fips_files(c64x.c)
    if (FIPS_IOS)
        fips_files(ios-info.plist)
    endif()
    fips_deps(roms common)
fips_end_app()
target_compile_definitions(c64-ui PRIVATE CHIPS_USE_UI)

fips_ide_group(examples/zx)
fips_begin_app(zx windowed)
    fips_vs_warning_level(3)
    fips_files(zx.c)
    if (FIPS_IOS)
        fips_files(ios-info.plist)
    endif()
    fips_deps(roms common)
fips_end_app()
fips_begin_app(zx-ui windowed)
    fips_vs_warning_level(3)
    fips_files(zx.c zx-ui.cc)
    if (FIPS_IOS)
        fips_files(ios-info.plist)
    endif()
    fips_deps(roms common ui)
fips_end_app()
target_compile_definitions(zx-ui PRIVATE CHIPS_USE_UI)

fips_ide_group(examples/cpc)
fips_begin_app(cpc windowed)
    fips_vs_warning_level(3)
    fips_files(cpc.c)
    if (FIPS_IOS)
        fips_files(ios-info.plist)
    endif()
    fips_deps(roms common)
fips_end_app()
fips_begin_app(cpc-ui windowed)
    fips_vs_warning_level(3)
    fips_files(cpc.c cpc-ui.cc)
    if (FIPS_IOS)
        fips_files(ios-info.plist)
    endif()
    fips_deps(roms common ui)
fips_end_app()
target_compile_definitions(cpc-ui PRIVATE CHIPS_USE_UI)

fips_ide_group(examples/kc85)
fips_begin_app(kc85 windowed)
    fips_vs_warning_level(3)
    fips_files(kc85.c)
    if (FIPS_IOS)
        fips_files(ios-info.plist)
    endif()
    fips_deps(roms common)
fips_end_app()
fips_begin_app(kc85-ui windowed)
    fips_vs_warning_level(3)
    fips_files(kc85.c kc85-ui.cc)
    if (FIPS_IOS)
        fips_files(ios-info.plist)
    endif()
    fips_deps(roms common ui)
fips_end_app()
target_compile_definitions(kc85-ui PRIVATE CHIPS_USE_UI)

fips_ide_group(examples/bombjack)
fips_begin_app(bombjack windowed)
    fips_vs_warning_level(3)
    fips_files(bombjack.c)
    if (FIPS_IOS)
        fips_files(ios-info.plist)
    endif()
    fips_deps(roms common)
fips_end_app()
fips_begin_app(bombjack-ui windowed)
    fips_vs_warning_level(3)
    fips_files(bombjack.c bombjack-ui.cc)
    if (FIPS_IOS)
        fips_files(ios-info.plist)
    endif()
    fips_deps(roms common ui)
fips_end_app()
target_compile_definitions(bombjack-ui PRIVATE CHIPS_USE_UI)

fips_ide_group(examples/pacman)
fips_begin_app(pacman windowed)
    fips_vs_warning_level(3)
    fips_files(pacman.c)
    if (FIPS_IOS)
        fips_files(ios-info.plist)
    endif()
    fips_deps(roms common)
fips_end_app()
fips_begin_app(pacman-ui windowed)
    fips_vs_warning_level(3)
    fips_files(pacman.c pacman-ui.cc)
    if (FIPS_IOS)
        fips_files(ios-info.plist)
    endif()
    fips_deps(roms common ui)
fips_end_app()
target_compile_definitions(pacman-ui PRIVATE CHIPS_USE_UI)

fips_ide_group(examples/pengo)
fips_begin_app(pengo windowed)
    fips_vs_warning_level(3)
    fips_files(pengo.c)
    if (FIPS_IOS)
        fips_files(ios-info.plist)
    endif()
    fips_deps(roms common)
fips_end_app()
fips_begin_app(pengo-ui windowed)
    fips_vs_warning_level(3)
    fips_files(pengo.c pengo-ui.cc)
    if (FIPS_IOS)
        fips_files(ios-info.plist)
    endif()
    fips_deps(roms common ui)
fips_end_app()
target_compile_definitions(pengo-ui PRIVATE CHIPS_USE_UI)

fips_ide_group(examples/lc80)
fips_begin_app(lc80 windowed)
    fips_vs_warning_level(3)
    fips_files(lc80.c lc80-ui.cc)
    if (FIPS_IOS)
        fips_files(ios-info.plist)
    endif()
    fips_deps(roms common ui)
fips_end_app()
