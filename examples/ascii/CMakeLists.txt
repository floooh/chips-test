find_package(Curses)
if (CURSES_FOUND)
    fips_begin_app(kc85-ascii cmdline)
        fips_files(kc85-ascii.c)
        fips_deps(kc85-roms curses)
    fips_end_app()
    fips_begin_app(c64-ascii cmdline)
        fips_files(c64-ascii.c)
        fips_deps(c64-roms curses)
    fips_end_app()
endif()
